export function helloWorld(): void {
  atom.notifications.addInfo(
    'Hello World!',
    {
      detail: atom.inDevMode()
        ? 'Package created with TypeScript'
        // eslint-disable-next-line
        : undefined
    }
  );
}
